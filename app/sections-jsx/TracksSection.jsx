'use strict';

var React = require('react');

var Start = React.createClass({

    render: function () {
        return (
            <section>{'Tracks'}</section>
        )
    }

});

module.exports = Start;