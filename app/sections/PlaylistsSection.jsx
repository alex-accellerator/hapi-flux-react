'use strict';

var React = require('react');
var TracksSection = require('./TracksSection');

var Start = React.createClass({
    render: function () {
        return (
            <section>{'Playlists'}</section>
        )
    }

});

module.exports = Start;